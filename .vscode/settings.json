{
  "cmake.buildDirectory": "${workspaceFolder}/build/${variant:platform}/${variant:buildType}",
  "cmake.generator": "Ninja",
  "python.formatting.provider": "black",
  "python.analysis.extraPaths": [
    "client/python"
  ]
}